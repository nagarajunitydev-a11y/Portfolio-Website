Place the actual Nestra thumbnail at `public/Images/NestraImage.png`.

If you have the production PNG, copy it to that path. Until then the portfolio will fall back to `/Images/placeholder.svg` if the image fails to load.
