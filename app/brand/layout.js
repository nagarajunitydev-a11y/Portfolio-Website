/** Internal exploration page — kept out of search and sitemaps. */
export const metadata = {
  title: 'Logo concepts',
  robots: { index: false, follow: false },
}

export default function BrandLayout({ children }) {
  return children
}
